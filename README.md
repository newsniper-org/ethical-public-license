# NEWSNIPER Ethical Public License (NEPL)

윤리적 제한을 건 **저작물·소프트웨어 공용 라이선스 템플릿**. 어느 저장소에나 그대로 붙여 쓸 수 있다.

> **초안 상태다 (freeze 전).** 문면은 바뀔 수 있고, 아직 법률 검토를 마치지 않았다.
> 이 문서는 법률 자문이 아니다.

---

## 무엇인가

| | |
|---|---|
| **코드** | 상업적 이용 허용, 동일조건 없음 (BSD-2 수준) |
| **글·그림·대본 등** | 비상업 한정 + 동일조건 (기본값. 적용 고지로 완화 가능) |
| **원작이 있는 2차 창작이면** | **원작 권리자가 역으로 가져다 쓸 때는 표기 의무가 권장으로 내려가고, 비상업·동일조건도 면제된다** (제4조) |
| **절대 금지** | 반인륜범죄 · 전쟁범죄 · 아동대상범죄 — 허락의 *범위 밖*으로 두어, 이 용도의 이용은 저작권 침해가 된다 (제5조) |
| **오픈소스인가** | **아니다.** 제5조의 이용 분야 제한 때문에 OSI 정의의 오픈소스도, FSF 정의의 자유 소프트웨어도 아니다. 제0조에서 그렇게 자인한다 |
| **CC·MIT·BSD의 변형인가** | 아니다. 독립된 문서이며 그 이름·상표를 쓰지 않는다 |

## 파일

```
NEPL-v1.ko.md    ← 정본 (한국어)
NEPL-v1.en.md    ← 번역 (영어)
NEPL-v1.ja.md    ← 번역 (일본어)
NEPL-v1.de.md    ← 번역 (독일어)

… 각 언어마다 같은 문면을 네 형식으로:
    .md   CommonMark
    .txt  UTF-8 plain text
    .rst  reStructuredText
    .typ  Typst >= 0.14

templates/  적용 고지(Application Notice) 빈 서식
tools/render.py   .md → .txt/.rst/.typ 생성기
```

**해석에 차이가 있으면 한국어본(`NEPL-v1.ko.md`)이 우선한다**(제10.1조).

형식이 넷인 이유는 편의뿐이다 — 제12.1조가 **「어떤 문서 형식으로 쓸지는 전혀 규정하지 않는다」**고 명시한다. 이 라이선스의 유일한 무조건 허용 조항이다. PDF든 HTML든 점자든 좋고, 문면만 바뀌지 않으면 된다.

## 쓰는 법

1. 라이선스 전문을 저장소에 넣는다(원하는 언어·형식으로, 몇 개든).
2. 최상위에 `LICENSE`(또는 `NOTICE`)를 두고 **적용 고지**를 채운다 — `templates/` 의 서식을 쓴다.
3. 끝. 저장소마다 달라지는 것(권리자·상위 저작물·별도 라이선스 부분·준거법·저작 부분의 상업 허용 여부)은 전부 적용 고지에 적고, 라이선스 본문은 손대지 않는다.

적용 고지에 **제5조를 완화하는 기재는 효력이 없다**(제12조).

### SPDX

표준 식별자가 없으므로 다음을 쓴다.

```
SPDX-License-Identifier: LicenseRef-NEPL-1.0
```

패키지 메타데이터(`pyproject.toml`, `package.json` 등)의 라이선스 필드에도 같은 문자열을 넣고, 파일 경로를 함께 적는다.

## 생성기

`.md` 가 정본이고 나머지 세 형식은 생성물이다. 손으로 고치지 않는다.

```sh
python3 tools/render.py          # 12개 파일을 다시 만든다
python3 tools/render.py --check  # 정본과 어긋난 생성물이 있는지, 형식 간 문면이 같은지 검사
```

`--check` 는 형식 간 문면 동일성까지 본다(구조 표지를 걷어내고 대조).

## 이 라이선스 문서 자체의 조건

제10.3조 — **원문 그대로 복제·배포할 수 있고, 누구든 자기 저작물에 적용할 수 있다.** 수정본을 만드는 것도 자유이나, 그때는 **「NEWSNIPER Ethical Public License」라는 이름과 「NEPL」이라는 약칭을 쓰지 않는다.**

## 이름

NEWSNIPER — 라이선스를 만든 사람의 활동명. 특정 조직의 상표가 아니다.

---

*NEPLv1 draft · 이 저장소는 라이선스 텍스트의 정본을 보관한다. 실제 적용 예는 각 저장소의 `LICENSE` 를 본다.*
